<div>
  <div class="col-md-4">
    <div class="row">
      <%= link_to gravatar_for(current_user), current_user %>
    </div>
    <br>
    <div class="row">
      <%= link_to current_user.name, current_user %>
    </div>
  </div>
  <div class="col-md-8">
     <span><%= link_to "view your profile", current_user %></span>
  </div>
</div>
<span><%= pluralize(current_user.posts.count, "posts") %></span>
